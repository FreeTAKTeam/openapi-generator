import { SubscriberDto } from '../../dtos/subscriber.dto';
import { ListResponse } from '@mvc/http/responses/list-response';
import { ListResultset } from '@mvc/data/list-resultset';

export class SubscribersListResponse extends ListResponse {
    constructor(list: ListResultset<SubscriberDto>) {
        super(list, 'SubscribersList');
    }
}
