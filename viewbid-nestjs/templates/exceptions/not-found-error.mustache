{{>licenseInfo}}

{{#models}}
{{#model}}
import { NotFoundViewbidError } from '@mvc/exceptions/not-found-viewbid.error';
import { {{classname}}Constants } from  '../config/{{classVarName}}.constants';

export class {{classname}}NotFoundError extends NotFoundViewbidError {
    constructor(id: string) {
        super('No {{classname}} found with id ' + id, {{classname}}Constants.{{#lambda.uppercase}}{{classVarName}}{{/lambda.uppercase}}_NOT_FOUND_ERROR, id, '{{classname}}#not-found');
    }
}
{{/model}}
{{/models}}
